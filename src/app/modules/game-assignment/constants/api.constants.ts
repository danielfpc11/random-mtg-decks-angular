import { environment } from '../../../../environments/environment';

export const DECK_URL: string = `${environment.serverUrl}/deck`;
export const GAME_URL: string = `${environment.serverUrl}/game`;
export const PLAYER_URL: string = `${environment.serverUrl}/player`;
